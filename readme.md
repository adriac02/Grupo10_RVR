Para utilizar GitHub Desktop en una máquina virtual de Ubuntu he tenido que descargar el paquete libcurl4, comando:<br>
    ```sudo apt-get install libcurl4```

<br>

Además de tener que descargar el compilador gcc con:<br>
```sudo apt install gcc```